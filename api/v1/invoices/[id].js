import { db } from "hatchable";
export const access="public";export const methods=["GET"];
function fail(res,s,c,m){return res.status(s).json({success:false,error:{code:c,message:m}})}
function uuid(v){return typeof v==="string"&&/^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i.test(v)}
export default async function(req,res){const id=req.params.id;if(!uuid(id))return fail(res,400,"VALIDATION_ERROR","Invalid invoice id");try{const {rows}=await db.query(`SELECT i.*,l.tenant_id,l.unit_id,t.name AS tenant_name,u.unit_number,p.name AS property_name,COALESCE((SELECT json_agg(ii ORDER BY ii.created_at) FROM invoice_items ii WHERE ii.invoice_id=i.id),'[]') AS items,COALESCE((SELECT json_agg(pm ORDER BY pm.payment_date DESC) FROM payments pm WHERE pm.invoice_id=i.id),'[]') AS payments FROM invoices i JOIN leases l ON l.id=i.lease_id JOIN tenants t ON t.id=l.tenant_id JOIN units u ON u.id=l.unit_id JOIN properties p ON p.id=u.property_id WHERE i.id=$1 GROUP BY i.id,l.tenant_id,l.unit_id,t.name,u.unit_number,p.name`,[id]);if(!rows[0])return fail(res,404,"NOT_FOUND","Invoice not found");return res.json({success:true,data:rows[0],message:"OK"})}catch(e){return fail(res,500,"INTERNAL_ERROR","Unable to fetch invoice")}}